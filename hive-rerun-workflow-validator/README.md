## Hive Rerun Workflow Validator

A CLI tool to validate workflow JSON by checking for duplicate IDs, missing dependencies, and cycles. It also prints a stable topological order.

### Usage

```bash
node index.js
```