// Compatibility bridge for the core barrel. The Project-LAL package is the only
// protocol definition; keeping this path prevents a disruptive CLI-wide import move.
export * from "@project-lal/protocol";
