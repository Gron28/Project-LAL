// Icon-size scale shared across the app — lucide icons stay directly imported,
// just sized from here instead of scattered inline literals (10-19px before this).
export const ICON_SIZE = { sm: 14, md: 16, lg: 20 } as const;
